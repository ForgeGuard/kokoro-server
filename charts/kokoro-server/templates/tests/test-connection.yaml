apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kokoro-server.fullname" . }}-test-connection"
  labels:
    {{- include "kokoro-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kokoro-server.fullname" . }}:{{ .Values.kokoroTTS.port }}']
  restartPolicy: Never
